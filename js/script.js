/**
 * Return Values Created by puhrome on 5/29/14.
 */

var total = calcArea(30, 20);//return function store in var total
//functions that are returning need to be assigned a variable
function calcArea() {//scope to calc area
    var area = w * h;
    return area;//function spitting info out
}
console.log(total);